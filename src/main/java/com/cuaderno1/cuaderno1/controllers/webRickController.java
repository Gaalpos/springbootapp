package com.cuaderno1.cuaderno1.controllers;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

import com.cuaderno1.cuaderno1.services.RickAndMortyService;

import ch.qos.logback.core.model.Model;

@Controller
public class webRickController {

    // @GetMapping("rickandmorty2")
    // public String getCharacter(Model model) {
    //     Character character = RickAndMortyService.getCharacter();
    //     if (character != null) {
    //         model.addAttribute("character", character);
    //         return "character"; // Devuelve el nombre de la plantilla HTML
    //     } else {
    //         return "error"; // Puedes tener una plantilla de error personalizada
    //     }
    // }
    
    
}
